{
  "name": "carrier-greenon",
  "version": "0.1.0",
  "private": true,
  "description": "캐리어 에어컨 사용자를 위한 ESG 친환경 냉방 미션 및 리워드 웹앱",
  "engines": {
    "node": ">=24.19.0 <25"
  },
  "scripts": {
    "check": "node --check app.js && node --check scripts/build.mjs && node --check scripts/scan-secrets.mjs",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "build": "npm run check && npm run scan:secrets && node scripts/build.mjs && npm run scan:secrets"
  }
}
